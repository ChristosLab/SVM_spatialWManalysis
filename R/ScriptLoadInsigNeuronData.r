isuspre0 <- RemoveZeroCols(ImportNeuronData("insigsuspre0"))
isuspre1 <- RemoveZeroCols(ImportNeuronData("insigsuspre1"))
isuspre2 <- RemoveZeroCols(ImportNeuronData("insigsuspre2"))
isuspre3 <- RemoveZeroCols(ImportNeuronData("insigsuspre3"))
isuspost0 <- RemoveZeroCols(ImportNeuronData("insigsuspost0"))
isuspost1 <- RemoveZeroCols(ImportNeuronData("insigsuspost1"))
isuspost2 <- RemoveZeroCols(ImportNeuronData("insigsuspost2"))
isuspost3 <- RemoveZeroCols(ImportNeuronData("insigsuspost3"))
iantpre0 <- RemoveZeroCols(ImportNeuronData("insigantpre0"))
iantpre1 <- RemoveZeroCols(ImportNeuronData("insigantpre1"))
iantpre2 <- RemoveZeroCols(ImportNeuronData("insigantpre2"))
iantpre3 <- RemoveZeroCols(ImportNeuronData("insigantpre3"))
iantpost0 <- RemoveZeroCols(ImportNeuronData("insigantpost0"))
iantpost1 <- RemoveZeroCols(ImportNeuronData("insigantpost1"))
iantpost2 <- RemoveZeroCols(ImportNeuronData("insigantpost2"))
iantpost3 <- RemoveZeroCols(ImportNeuronData("insigantpost3"))

ieverything0 <- RemoveZeroCols(ImportNeuronData("insigeverything0"))
ieverything1 <- RemoveZeroCols(ImportNeuronData("insigeverything0"))
ieverything2 <- RemoveZeroCols(ImportNeuronData("insigeverything0"))
ieverything3 <- RemoveZeroCols(ImportNeuronData("insigeverything0"))